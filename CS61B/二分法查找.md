#algorithm
```java
static int binarySearch(String[] sorted, String x, int lo, int hi)
if (lo > hi) return -1;
int m = (lo + hi) / 2;
int cmp = x.compareTo(sorted[m]);
if (cmp < 0) return binarySearch(sorted, x, lo, m - 1);
else if (cmp > 0) return binarySearch(sorted, x, m + 1, hi);
else return m;
```

利用二分法进行有序列表的查找元素 N = hi - lo + 1
1. cost model:Number of BinarySearch calls
2. C(N) = ⌊log2(N)⌋+1
3. R(N) = Θ(⌊log2(N)⌋) --->Θ(log N) 对数运算中底数不影响order of growth
## selection sort VS Merge sort
Θ(NlogN) much better than N^^2
==this is the rudimentary logic structure of [[BST]]==